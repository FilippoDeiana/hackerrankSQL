(SELECT  concat(NAME, "(", LEFT(OCCUPATION, 1), ")") AS OCC
FROM OCCUPATIONS 
ORDER BY NAME)
UNION ALL
(SELECT concat("There are a total of ",COUNT(OCCUPATION)," ",LOWER(OCCUPATION),"s.")
FROM OCCUPATIONS 
GROUP BY OCCUPATION 
ORDER BY count(OCCUPATION), OCCUPATION) ORDER BY OCC
